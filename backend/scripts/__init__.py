"""Standalone operational scripts for the demo environment.

Run as `python -m scripts.<name>` from `backend/` (docs/17-roadmap.md's
Phase-2 demo surface, e.g. `python -m scripts.seed`). This package is
never imported by `app/*` at runtime — the dependency arrow points one
way, script -> app, never back.
"""
